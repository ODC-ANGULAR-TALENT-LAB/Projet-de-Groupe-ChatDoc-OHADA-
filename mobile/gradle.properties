org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
# Le build tourne en integration continue : le demon Gradle n'y sert a
# rien et consomme de la memoire pour un processus qui ne survit pas.
org.gradle.daemon=false
